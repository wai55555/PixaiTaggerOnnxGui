# Development / CI dependencies for environments without an NVIDIA CUDA stack
# (WSL, Linux CI, non-NVIDIA Windows dev machines).
#
# Identical to requirements.txt except the CPU-only 'onnxruntime'. The GPU code
# path (onnx_providers.resolve_providers) resolves to CPUExecutionProvider here
# because CUDAExecutionProvider is not offered by this build, so behaviour is
# unchanged. The frozen release build must use requirements.txt (onnxruntime-gpu).
#
# Keep the non-onnxruntime lines below in sync with requirements.txt.
numpy==1.26.4
pillow==12.0.0
onnxruntime==1.23.2
requests==2.32.5
PySide6==6.10.0
tokenizers==0.23.1
keyring==25.5.0
